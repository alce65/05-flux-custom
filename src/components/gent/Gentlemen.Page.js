import GentList from './Gent.List';

export function GentlemenPage() {
    const template = (
        <section className="gentelman-page">
            <GentList />
        </section>
    );

    return template;
}
